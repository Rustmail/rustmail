[package]
name = "rustmail_types"
version = "1.0.17"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
chrono-tz = { version = "0.10.4", features = ["serde"] }

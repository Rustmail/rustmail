[package]
name = "rustmail_types"
version = "0.1.1"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono-tz = { version = "0.10", features = ["serde"] }
